# The Plex persistence layer emits branded validation errors that begin with
# the product proper noun "Plex". Preserve that wording while inheriting every
# other Staticcheck rule for the db package.
checks = ["inherit", "-ST1005"]
