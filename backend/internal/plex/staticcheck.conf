# Plex is a product proper noun and these errors can be surfaced directly in
# API/UI diagnostics. Preserve the branded capitalization while inheriting all
# other repository Staticcheck checks, including correctness checks.
checks = ["inherit", "-ST1005"]
